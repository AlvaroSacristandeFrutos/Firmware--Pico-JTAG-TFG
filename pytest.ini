[pytest]
# Tests que funcionan con solo la Pico conectada (sin target JTAG).
# Uso: python -m pytest   (desde la raiz del proyecto)
testpaths =
    test/test_firmware_completo.py
    test/test_casos_limite.py
    test/test_gaps_cobertura.py
    test/test_nuevos_casos_limite.py
    test/test_set_tms_y_limites_dma.py
